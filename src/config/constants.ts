export const APP_CONFIG = {
  LOADING_DELAY_MS: 500,
  MAX_PRODUCTS_PER_EXERCISE: 4,
  PROFILE_EXPIRATION_DAYS: 30,
  SHARE_LINK_COPIED_TIMEOUT: 3000,
  FORM_CLEANUP_DELAY: 100,
} as const;

export const EXTERNAL_WEBSITE_NAME = "NuitInfo";

export const DEFAULT_AGE = 30;
export const DEFAULT_NIVEAU = "debutant" as const;
export const DEFAULT_FREQUENCE = "0" as const;
